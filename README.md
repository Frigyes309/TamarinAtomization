# TamarinAtomization

You need M4 installed to run the codes.

After running the `./run.sh` you will se files in the compiled folder.

Start tamarin by using:
```bash
tamarin-prover interactive <insert filename>.spthy
```